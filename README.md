# Student data storage system
A simple program that displays student names and grades. Program lets user add and delete students, search by grades and output all the students' grade averages. Data is stored in an unrolled linked list.
# Starting the program
To start the program run the project in NetBeans. Sample data is included in the data.TXT file.
